# 模板验证记录 · v1.3.0

验证日期：2026-09-13。
基线：GitHub main 提交 `1a783227faac9d31008a67549a3de2dfe7cdbd99`，41 个文件逐一核对 Git blob SHA 后创建工作副本。
环境：Linux，Python 3.12.14。

## 变更范围

精简 AGENTS.md、按需资料索引与提示；区分初始化、业务开发和简单修改；收紧测试范围及停止条件；缩短任务模板；统一审查标准并明确主 Agent 在授权内继续修复交付；补充可选 Skills 与指令评估说明。
保留第一性原理、按风险审查、重要行为测试、真实集成验证、权限/隔离和诚实证据要求。业务代码、检查脚本、验证配置、CI 配置均未修改。

## 实际验证

| 检查 | 结果 | 范围 |
|---|---|---|
| `python3 scripts/check_template.py` | 通过 | 必备文件、配置基本结构、本地 Markdown 文件链接；不验证外部链接及标题锚点 |
| `python3 scripts/test_framework.py` | 13 项通过 | 临时副本中的框架回归，含 8 个示例测试、故障注入、配置/失败传播、任务创建及生成后链接检查 |
| `git diff --check` | 通过 | 差异空白检查 |

故障注入和未配置 quick/full 的失败是回归中的预期结果，不是业务检查通过。任务创建测试覆盖替换模板参数、不覆盖已有文件与拒绝路径穿越。
文档由本次主 Agent 自审：核对条件化读取、风险规则、审查引用、授权边界、测试停止条件与完成定义；不是独立人员或第二模型审查。

## 限制与下一步

- 未执行模型行为 A/B 对照，不宣称耗时、token 或正确率已经改善；评估方法见 [指令维护](docs/instruction-maintenance.md)。
- 未安装或测试第三方 Skills，未执行真实子 Agent 调度与跨工作区整合。
- 未执行真实业务、数据库、浏览器、小程序真机或 Windows/macOS 验证；模板仍未接入业务命令。
- 远程 CI 不在本地验证中预先声明通过，结果以包含本文件的提交所对应 GitHub Actions 记录为准。

关联提交：本文件所在的 v1.3.0 更新提交（精确 SHA 由 Git 历史定位，避免自引用提交号）。
下一步：接入具体业务需求与真实验证命令；有代表任务后比较修改前后的 Agent 行为。
MANIFEST.sha256 记录本版本其他交付文件的 SHA-256，修改文件后需要重新生成；历史验证记录保留在 Git 历史中。
