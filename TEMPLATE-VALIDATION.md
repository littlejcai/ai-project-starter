# 模板验证记录 · v1.7.0

日期：2026-09-14；环境：macOS，Python 3.9.6。
基线：GitHub main `dc30653989c844f0f9b52c64ddb4830847388cc9` 加本次 v1.7.0 工作区；最终发行文件由 `MANIFEST.sha256` 逐一校验 LF 规范化内容哈希。

## 变更与审查

模板不再预建 `src/`、`tests/`、`contracts/`、`adapters/` 四个说明型目录。源码、测试、接口契约、迁移与外部适配器在项目技术方案确定后按真实结构创建；原接口约定与跨 AI 使用规则已分别收回 `docs/architecture.md`、README 和 AGENTS.md。
结构检查不再要求具体的契约目录；新增回归用例确认干净模板没有上述推测性目录。生命周期、门禁、任务、按需读取、报告证据、严格清单及 CI 行为保持不变。

## 实际验证

| 检查 | 结果 | 范围 |
|---|---|---|
| `python3 scripts/test_framework.py` | 34 项通过 | 原有 33 项保持通过，新增无推测性项目目录回归 |
| `python3 scripts/verify.py --profile current --readiness --strict-manifest` | 通过 | 当前 template 配置/任务/结构/链接/发行清单；不代表业务通过 |
| `git diff --check` | 通过 | 差异空白检查 |

回归同时覆盖既有配置错误传播、阶段推进、任务保护、报告读取边界、目录遍历与清单失败；目录收敛没有放宽任何必需门禁。

## 限制与交接

这些结果证明模板结构和工具行为，不代表已测得模型 token、耗时或正确率改善。未运行真实业务/数据库/设备验收，也未增加目录生成器、调度层或测试缓存。
报告仍需结合整个 workflow 状态；远程 CI 及摘要归档以本提交对应 GitHub Actions 记录为准，不在本地预先声明通过。
关联提交：本文件所在的 v1.7.0 更新提交。下一步：在真实任务中观察按需目录是否减少错误定位和无效维护，保留既有必要验收。
