# 模板验证记录 · v1.2.0

验证日期：2026-09-12。
环境：当前 Linux 容器，Python 3.12.14。未在 Windows/macOS 执行，未运行远程 GitHub Actions。

## 实际结果

| 项目 | 结果 | 范围 |
|---|---|---|
| `python3 scripts/check_template.py` | 通过 | 必备文件、配置基本结构、本地 Markdown 文件链接；不检查外网链接或标题锚点 |
| `python3 scripts/verify.py --profile demo` | 通过，8 个测试 | 独立报名资格规则，不含数据库或界面 |
| `python3 scripts/test_framework.py` | 通过，13 项，约 1 秒 | 在临时副本中验证框架工具 |
| 故障注入 | 预期失败，捕获 2 个断言失败 | 满员及容量零仍返回 ACCEPT 的错误被发现 |
| quick/full 默认状态 | 预期失败 | 业务命令未配置时不会给出通过结论 |

13 项框架回归覆盖：模板与示例、故障注入、未配置 quick/full、已配置执行与报告、非零退出码、超时、命令不存在、空命令、全部不适用、缺少不适用理由、非法 JSON、失效链接、创建任务/拒绝覆盖/拒绝路径穿越。
测试中使用的 `python -c pass` 只用于验证命令运行器，不是正式业务检查配置。

## 明确限制

- 尚未选择真实业务技术栈；没有真实服务端、数据库、浏览器或小程序功能。
- GitHub Actions 文件只配置模板与示例检查，远程运行待用户仓库接入后确认。
- 命令正确退出不自动证明测试充分；配置变化与测试预期仍需审查。
- 模板的自动检查不能代替真实业务验收、真机测试和发布验证。

MANIFEST.sha256 列出本版本其他交付文件的 SHA-256；修改文件后需重新生成，Git 提交历史负责版本追踪。

## v1.1.0 本次更新验证

基于 GitHub main 提交 190afee1b94de96432cb31988d86cc6552c58029 的文件快照。
新增原则、风险分级、审查流程及提示；更新任务、决策、产品、架构与示例说明。业务代码、检查脚本和 CI 配置未改动。

- 本地必备文件及 Markdown 文件链接检查：通过。
- 独立 demo：8 个规则测试通过。
- 框架回归：13 项通过，包含任务创建后的链接检查、默认未配置拒绝通过、故障注入产生 2 个预期断言失败。
- 人工方式自审文档：核对风险分级、审查停止条件、任务/决策/提示引用及示例边界；不是独立人员或第二模型审查。
- 未执行：真实业务集成、浏览器、小程序真机；本地记录不预先声明远程 CI 已通过。

本次为流程文档变更，没有新增低价值的文字镜像测试，也没有把 AI 自评接入自动放行。

## v1.2.0 本次更新范围

基于 GitHub main 提交 983030017b0ee3acce3ee3900d5b3697c828d734 的文件快照。
新增可选子 Agent 协作说明和审查委派提示，接入入口、索引、任务、流程和审查说明。业务代码、检查脚本和 CI 配置不变。
本次为主 Agent 自审文档，未启动子 Agent；交付的是协作约定与模板，没有宣称已实现或验证自动调度能力。
真实子 Agent 调用、跨工作区合并、独立模型审查以及真实业务/设备验证不在本次执行范围。
本地文档结构与链接检查通过；13 项框架回归通过，其中包含 8 个示例规则测试、故障注入和任务创建后的链接检查。远程 CI 结果以对应 GitHub Actions 运行记录为准。
