# 模板验证记录 · v1.8.0

日期：2026-09-15；环境：macOS，Python 3.9.6。
基线：GitHub main `867d43e83ec6188cded66f5a4e0b1027a8caf1a3` 加本次 v1.8.0 工作区；最终发行文件由 `MANIFEST.sha256` 逐一校验 LF 规范化内容哈希。

## 变更与审查

启动提示现在显式收集界面形态与已有品牌/设计资产；产品定义保留最小入口，开发流程规定仅在存在实际界面交付时创建单一 `docs/experience.md`。无界面项目、短期实验和已有成熟规范分别采用不创建、任务内记录或薄引用方式；只有独立治理或单文件妨碍定位时才拆分品牌与 UX/UI 文档。
模板仍不预建体验、品牌、UX/UI、源码、测试、契约或适配目录，也不增加目录生成器、配置字段或技术/审美绑定。新增回归用例锁定“默认不存在、触发后按需物化”的边界；生命周期、门禁、任务、报告证据及 CI 行为保持不变。

## 实际验证

| 检查 | 结果 | 范围 |
|---|---|---|
| `python3 scripts/test_framework.py` | 35 项通过 | 既有 34 项保持通过，新增按需体验基线回归 |
| `python3 scripts/verify.py --profile current --readiness --strict-manifest` | 通过 | 当前 template 配置/任务/结构/链接/发行清单；不代表业务通过 |
| `git diff --check` | 通过 | 差异空白检查 |

回归继续覆盖既有配置错误传播、阶段推进、任务保护、报告读取边界、目录遍历与清单失败；体验入口没有放宽任何必需门禁，也不把文档存在当作界面验收。

## 限制与交接

这些结果只证明模板结构和工具行为，不代表已测得模型 token、耗时、设计质量或业务正确率改善。未运行真实业务、浏览器、数据库或设备验收，也未增加目录生成器、调度层或测试缓存。
报告仍需结合整个 workflow 状态；远程 CI 及摘要归档以本提交对应 GitHub Actions 记录为准，不在本地预先声明通过。
关联提交：本文件所在的 v1.8.0 更新提交。下一步：在真实界面任务中观察单一体验基线能否减少重复猜测，并以是否真实约束实现、审查或验收决定是否保留或拆分。
