# 模板验证记录 · v1.6.0

日期：2026-09-14；环境：Linux，Python 3.12.14。
基线：GitHub main `19d8e28893ac653c4f8de7461ce795c7d7a10041`，43 个文件已逐一核对 Git blob SHA。

## 变更与审查

任务增加具体文件/符号、规则和测试定位，索引定位到文档章节。read_doc.py 默认列标题或返回有界章节，不递归跟随链接。
verify.py 复用报告读取入口，默认只返回失败/警告摘要，显式 --check/--tail 才展开指定诊断与有界日志。读取不重跑检查，不改变原报告或任务。
--readiness 合并 doctor 补充检查，--strict-manifest 包含模板发行哈希。CI 改为组合调用，保留配置、任务、结构、链接、阶段门禁、清单与框架回归；没有跨运行缓存或公开跳过检查开关。
目录遍历在进入依赖/构建目录前排除它们，结构检查与清单共用遍历规则；未改为只检查变更文件。主 Agent 自审了检查职责、错误传播、输出边界、读取路径及新旧调用兼容性。

## 实际验证

| 检查 | 结果 | 范围 |
|---|---|---|
| `python3 scripts/test_framework.py` | 33 项通过 | 保留原有 25 项，新增组合调用、只读读取与遍历边界验证 |
| `python3 scripts/verify.py --profile current --readiness --strict-manifest` | 通过 | 当前 template 配置/任务/结构/链接/发行清单；不代表业务通过 |
| `python3 scripts/read_doc.py docs/testing.md --section "范围与停止条件"` | 通过 | 只返回目标章节及来源行号 |
| `git diff --check` | 通过 | 差异空白检查 |

回归用调用计数验证：一次组合调用的配置加载、校验、文档结构遍历各一次；清单仍单独做必要哈希校验。错误任务状态或清单不一致仍失败，配置结构错误得到受控失败。
读取验证覆盖：默认不泄露原始日志/配置；显式日志展开有长度限制；读取失败报告不改原结论或生成新运行；拒绝路径越界、歧义/缺失标题；忽略围栏内伪标题并支持章节续读。遍历测试确认不进入排除目录。

## 限制与交接

这些结果证明工具行为与输出范围，不代表已测得模型 token、耗时或正确率改善。未运行真实业务/数据库/设备验收，也未增加调度层、测试缓存或重做测试夹具。
报告仍需结合整个 workflow 状态；远程 CI 及摘要归档以本提交对应 GitHub Actions 记录为准，不在本地预先声明通过。
关联提交：本文件所在的 v1.6.0 更新提交。下一步：在真实任务中比较检索、读取和排障次数及可获取用量，保留既有必要验收。
