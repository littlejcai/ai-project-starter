# 不同 AI 工具的入口适配

通用知识放在 AGENTS.md 与 docs/。先检查当前客户端是否会自动读取项目入口。
若工具支持自己的规则文件，让该文件指向 AGENTS.md；按工具当前官方格式配置，不复制整套业务说明。
若工具无法直接读仓库，需要提供入口与当前任务关联文件，或者为它接入文件访问能力。
根目录 CLAUDE.md 提供一条指向公共入口的说明；是否实际加载须在所用客户端确认。
不要因换模型而重写项目知识。首次使用可让 AI 简述读到的项目目标、检查命令和当前任务以核对加载情况。
