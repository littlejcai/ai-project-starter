# 项目 AI 开发入口

## 项目身份与状态

当前是技术栈中立的开发模板，不是已实现的业务项目。
业务目标与范围见 [产品定义](docs/product.md)，架构见 [技术方案](docs/architecture.md)，当前进度见 [状态](docs/status.md)。
业务接入后将本段更新为真实项目的一句话介绍，避免继续沿用模板身份。

## 开始任务

1. 阅读本文件、docs/status.md、当前任务；通过 [索引](docs/index.md) 查找相关规则、约定和测试。
2. 先检查工作区变更、已有实现与测试；保留用户已有工作。
3. 用简短计划说明目标、验收条件、涉及范围；沿用已经确认的需求和授权。
4. 常规可逆实现自主推进；影响业务含义的歧义先做能做的工作，再集中确认。不要反复确认已授权事项。
5. 文档、代码、测试矛盾时指出具体差异并核实，不凭便利选择一个作为正确答案。

## 每个功能循环

- 以完整可验收的小业务链路为任务，先列正常、异常、边界、权限场景。
- 对复杂规则和重要状态先编写针对行为的测试，运行确认因缺少预期行为而失败，再实现、通过、重构。
- 简单文案、样式与探索性原型按风险选择验证，不强制为无意义内部细节编写测试。
- 测试预期依据业务规则；不为通过而删除断言、跳过测试或篡改预期。修正测试要记录业务或测试依据。
- 模拟依赖适合快速测试；不能替代真实数据库、权限、事务与平台验证。
- 界面变更及时运行浏览器或开发者工具检查；需要真机的场景明确记录。
- 不扩展未提出的产品范围；重要架构取舍记录在 docs/decisions/。

## 决策与审查原则

- 第一性原理：重要需求/架构先明确目标、已知事实与硬约束、待验证假设、最小方案和关键实验；沿用有效结论，普通小改动不重做分析。
- 对抗性审查：主动找失败条件，同时审查测试；按 [审查流程](docs/review.md) 记录位置、触发条件、影响及证据，区分缺陷、风险与建议，不凑问题数量。
- 简单设计：只为当前确认需求增加复杂度；必要权限、测试、数据一致性和维护工作不能省略。
- 契约与不变量：明确操作前提、结果和必须成立的业务条件，注明生效边界并映射到约束与测试。
- 证伪与最小实验：优先验证高影响、低把握假设；关键测试应能捕获错误实现，按需在隔离环境找反例。
- 小批量与可回退：交付完整小链路，难回退的数据/外部操作提前明确恢复或补偿办法。

风险规则：低风险做影响核对和预览/回归；普通业务做一次实现审查；高风险在方案与实现阶段分别审查。默认不强制多 Agent。
详细方法按需读 [开发原则](docs/principles.md)。结论记录在现有任务或决策中，不要求长篇推导；范围已检查、阻塞项已处置且验证充分后结束。

## 已有命令

```bash
python3 scripts/check_template.py
python3 scripts/verify.py --profile demo
python3 scripts/verify.py --profile quick
python3 scripts/verify.py --profile full
python3 scripts/new_task.py FEAT-001 "任务标题"
```

启动业务服务的命令尚未配置，见 project.config.json。quick/full 未配置时应失败；不要用 demo 替代业务检查。
验证日志在 artifacts/，不提交原始敏感日志。依赖、安装、运行版本由业务接入时填入 README 与技术方案。

## 读取与写回

按需读取当前任务的规则、模块和测试，不默认加载全部文档、历史任务或聊天记录。
接口格式维护在 contracts/，业务含义维护在 product.md，实际迁移维护在选定的迁移目录；其他位置引用，避免多份字段清单。
任务结束更新任务状态、实际验证、未执行项、已知问题和下一步；记录关联提交（没有提交就明确写无）。

## 完成标准

满足验收场景；相关检查通过；界面与集成证据充分；文档同步；未执行项明确。
只汇报实际执行过的命令与结果。自动检查通过不等于真机验收或发布完成。
