# 项目 AI 开发入口

## 项目事实

当前是技术栈中立的开发模板，尚无真实业务应用。业务接入后更新本段身份与运行信息。
业务含义以 [产品定义](docs/product.md) 为依据，接口格式维护在 contracts/，结构约束见 [技术方案](docs/architecture.md)，实际迁移维护在选定的迁移目录；其他位置引用，避免重复维护。

## 工作与授权

先核对用户目标、相关实现/测试与工作区变更，保留用户已有工作。已有任务直接沿用；简单局部修改无需另建任务或输出计划，复杂工作用简短计划明确验收与范围。
在已有授权内持续完成实现、必要修复、验证与交付，不重复确认已授权事项。涉及业务含义的重大歧义或超出授权的不可逆操作，先完成可推进部分，再集中说明需要决定的事项及原因。
不扩大产品范围；文档、代码、测试矛盾时核实依据，不为通过检查篡改测试预期或跳过要求。重要架构取舍记录在 docs/decisions/。

## 按需读取

已在上下文中的有效资料无需重复读取；仅在缺少信息或资料变化时补读。链接是查阅入口，不要求递归加载所有关联文件。

- 新项目初始化或业务功能开发：查 [流程](docs/workflow.md)；复杂规则测试、验证范围不明时查 [测试策略](docs/testing.md)。
- 新需求、重要架构选择或反复失败：查 [原则](docs/principles.md)，记录关键假设、约束与验证结论；普通小改动不重做推导。
- 低风险变更做影响核对与针对性验证；普通业务做实现审查，高风险做方案与实现审查，标准见 [审查流程](docs/review.md)。
- 有范围清晰、可独立完成且预期收益超过协调成本的子任务时，在工具和授权允许的情况下按 [协作约定](docs/agents.md) 委派；主 Agent 负责整合与最终验收。
- 接续任务或核对跨任务进度时查 [状态](docs/status.md)；其他资料通过 [索引](docs/index.md) 定位。

## 命令与验证

从仓库根目录运行，需要 Python 3.10+，仅用标准库：

```bash
python3 scripts/check_template.py
python3 scripts/verify.py --profile demo
python3 scripts/verify.py --profile quick
python3 scripts/verify.py --profile full
python3 scripts/new_task.py FEAT-001 "任务标题"
```

以上是命令目录，不要求每项任务全部运行。模板文档变更运行结构/链接检查；修改框架工具或任务模板时再运行 `python3 scripts/test_framework.py`。业务变更执行相关验证及项目/CI 要求的检查。
业务启动与 quick/full 尚未在 project.config.json 配置，未配置时应失败；demo 不能替代业务检查。接入说明见 [验证配置](docs/verification-setup.md)。
仅在新变更、失败或具体未解决风险需要时扩大或重复验证；既有必需检查不得省略。涉及界面、数据库/权限/事务或平台能力的变更，完成对应的真实环境验证；模拟结果不能替代它们。
日志在 artifacts/，原始敏感日志、密钥和生产数据不提交。

## 完成与交接

验收满足、必需检查通过、相关审查阻塞项已处置、受影响文档同步后交付。任务记录实际命令与结果、未执行项、已知问题及下一步；跨任务状态变化才更新 docs/status.md，小改动可在提交说明或回复中记录。
只声明实际执行过的验证；缺环境就明确限制，不将未完成项写成通过。自动检查不等于真机验收或发布；用户要求的提交、推送等已授权动作也属于交付范围。
